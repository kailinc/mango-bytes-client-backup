<div class="row">
  <h1>Products </h1>
  {{#each items as |item|}}
    <div class="col-sm-4">
      <img src="{{item.img}}" style="width: 100px;">
      <h3>{{item.name}}</h3>
    </div>
  {{/each}}
</div>
